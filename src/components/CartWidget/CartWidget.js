import './CartWidget.css';
import {AiOutlineShoppingCart} from 'react-icons/ai'

const CartWidget = () => {
  return (
    <AiOutlineShoppingCart id="cart" size={30}/>
  );
}

export default CartWidget
